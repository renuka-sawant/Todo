# We strongly recommend using the required_providers block to set the
# Azure Provider source and version being used
// Provider configuration moved to root `parents/provider.tf` to allow caller-level
// dependencies and avoid legacy module behavior. Keep module provider-free.

